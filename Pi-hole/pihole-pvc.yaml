apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-pv-claim
  namespace: pihole
  labels:
    app: pihole
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
