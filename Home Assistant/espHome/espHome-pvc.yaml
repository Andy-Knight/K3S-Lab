apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome-pv-config
  namespace: home-assistant
  labels:
    app: esphome
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi